# מניין הצעירים · מעלה עמוס — האתר

אתר סטטי, בלי build ובלי תלויות. כל דבר שאפשר לחשב — מחושב בדפדפן;
כל דבר שצריך אדם שיקבע — יושב בקובץ JSON אחד ב-`data/`.

**הכתובת הרשמית:** https://minyan.mokad.co.il
**כתובת גיטהב (גיבוי, תמיד עובדת):** https://6787012-max.github.io/minyan-tzeirim/
**Repo:** `6787012-max/minyan-tzeirim` · Pages מהשורש של `main`

## הדומיין

`mokad.co.il` נרכש ב-30/08/2026 ב-LiveDNS לחמש שנים. הוא **דומיין־גג לכל
הפרויקטים**, לא רק למניין — תת־כתובת לכל אחד:

| תת־כתובת | לאן |
|---|---|
| `minyan.mokad.co.il` | האתר הזה ✅ **חי, HTTPS, מאושר בנטפרי** |
| `mechina.mokad.co.il` | בית התלמוד (עדיין לא הוגדר) |
| `gmachim.mokad.co.il` | מרכז הגמ"חים (עדיין לא הוגדר) |
| `hoze.mokad.co.il` | דף החתימה (עדיין לא הוגדר) |

**להוסיף תת־כתובת חדשה** — שני צעדים בלבד:
1. בפאנל LiveDNS (`domains.livedns.co.il` ← ניהול DNS): רשומת `Alias (CNAME)`,
   Host = השם בלבד (`mechina`), Target = `<user>.github.io` **בלי https ובלי סלאש**.
2. בריפו היעד: קובץ `CNAME` בשורש עם הכתובת המלאה, ו-
   `PUT /repos/<owner>/<repo>/pages` עם `{"cname": "..."}`.

> ✅ **נטפרי אישרו את `mokad.co.il`** ב-30/08/2026 (פנייה 6194852, נעמה):
> *"האתר נפתח — עם סינון מלל על ידי רובוט אוטומטי וסינון תמונות אנושי."*
> האישור חל על הדומיין כולו, כך שכל תת־כתובת שנוסיף עוברת בלי פנייה נוספת.
> מה שנדרש בפנייה: התחייבות שהתוכן יתאים למדיניות + פירוט נושא האתר +
> קישור לתוכן הקיים בדומיין שכבר מאושר, כדי שיוכלו לראות ולא רק להאמין.

> ⚠️ **סדר הפעולות בחיבור דומיין — לא לשבור אותו כמו שקרה כאן.**
> להגדיר `custom domain` בגיטהב **רק אחרי** שה-CNAME כבר עונה ב-DNS.
> אם מגדירים מוקדם, גיטהב מתחיל להפנות 301 מהכתובת שעובדת אל כתובת שעדיין
> לא קיימת — והאתר יורד לגמרי.

## החשבון שנחסם — רקע

`maninhatzairimmaalaamus` (נפתח 30/08) **סומן ע"י גיטהב תוך שעות**: הפרופיל
מחזיר 404 למי שלא מחובר, Actions מושבת (`"Actions has been disabled for this
user"`) ו-Pages לא בונה. גיטהב אישרו את זה במילים שלהם בדף
`support.github.com/verification`: *"חשבונך סומן"*.

**הסיבה:** כל הפעילות בשעה הראשונה נעשתה דרך ה-API — טוקן עם הרשאות אדמין
11 דקות אחרי פתיחת החשבון, ואז יצירת ריפו, דחיפת 40 קבצים ושינוי שם — בלי שום
פעילות בדפדפן. זה דפוס של בוט. **הלקח: בחשבון גיטהב חדש, לפתוח את הריפו הראשון
ידנית בדפדפן ולמלא פרופיל לפני שנוגעים ב-API.**

הדומיין הפרטי הופך את כל הסיפור ללא רלוונטי — הכתובת כבר לא תלויה בשם החשבון.

---

## הדפים

| דף | מה יש בו |
|---|---|
| `index.html` | פתיח · פס "היום" (תאריך עברי, נץ, שקיעה, השיעור הבא) · זמני תפילות ושיעורים · זמני שבת + 8 השבתות הקרובות · מגבית חיה · אודות |
| `shas.html` | חלוקת ש״ס שוטנשטיין — 73 כרכים, מי לקח מה, וקישור תרומה לכל כרך |
| `hok.html` | הוראת קבע בנקאית (מס״ב) — שלושת המסלולים, מילוי דיגיטלי של הטופס המקורי, והמסמכים להורדה |

## איפה משנים תוכן

**כל התוכן ב-`data/`. אין טקסט קשיח ב-HTML.**

| קובץ | מה בפנים |
|---|---|
| `data/config.json` | שם, כתובת, זמני תפילות ושיעורים, מרווחי הדלקת נרות, פרטי נדרים פלוס, טקסט האודות |
| `data/shas.json` | 73 הכרכים. **לסמן שכרך נתפס = למלא את השדה `by` בשם התורם** |
| `data/yamim_noraim.json` | מכירת מקומות לימים הנוראים. `draft: true` עד שהמחירים מאושרים |
| `data/shabbat.json` | פרשיות ומועדים — **נוצר אוטומטית, לא לערוך** |
| `data/hok.json` | מפת שדות טופס המס״ב — **נוצר אוטומטית, לא לערוך** |

## מה מחושב בדפדפן

`js/luach.js` — בלי ספריות חיצוניות:

- **תאריך עברי** — אריתמטיקה טהורה. אומת מול `pyluach` על **22,280 ימים (2005–2065), אפס סטיות**.
- **נץ החמה ושקיעה** — האלגוריתם המלא של NOAA לפי הקואורדינטות של מעלה עמוס.
  אומת מול מימוש עצמאי בפייתון: **סטייה מרבית 0.01 דקה על 730 ימים**.
- **שעון קיץ ישראלי** — מיום שישי שלפני יום ראשון האחרון במרץ עד יום ראשון האחרון באוקטובר.
- **תקופת הסליחות** (נוסח אשכנז) — מיום ראשון שלפני ר״ה, ואם נותרו פחות מ-4 ימים אז שבוע קודם.
  נבדק על 50 שנה מול כל ארבעת ימי ר״ה האפשריים. בתקופה הזו האתר **מציג אוטומטית** את שעות הסליחות.

**מה לא מחושב, בכוונה:** שמות הפרשיות. החישוב תלוי בסוג השנה ובמועדים שנופלים
בשבת — מקום קלאסי לשגיאה שקטה. במקום זה `tools/gen_luach.py` מקפיא טבלה
מ-`pyluach` ל-20 שנה קדימה.

## הכלים

```bash
python tools/gen_luach.py 2046     # data/shabbat.json — פרשיות ומועדים
python tools/gen_fonts.py          # fonts/*.woff2 — גופנים מצומצמים (56KB לכולם)
python tools/gen_hok_form.py       # img/hok-form.png + data/hok.json
```

## פריסה

```bash
git add -A && git commit -m "..."
push.cmd        # דוחף לשני הריפו — הפעיל והחדש
```

GitHub Pages מגיש את השורש של `main`. יש `.nojekyll`, כך שתיקיות עם קו תחתון עוברות.
**אחרי כל דיפלוי שמשנה קבצים — להעלות את `V` ב-`sw.js`**, אחרת ה-service worker
יגיש למי שכבר ביקר את הגרסה הישנה.

## החלטות שכדאי להכיר לפני שמשנים

- **גופנים ארוזים באתר ולא מ-Google Fonts.** נטפרי חוסם דומיינים לא מאושרים,
  ואתר שתלוי בגופן חיצוני נראה שבור. הגופנים חופשיים להפצה (OFL / Culmus).
- **`GetMosad` של נדרים נשמר לשעה ב-localStorage.** נדרים מגבילים ל-~100 פניות
  ב-10 דקות לכל IP ומבקשים במפורש לא לקרוא בכל טעינת דף.
- **טופס המס״ב הוא תמונה של הטופס המקורי**, והמילוי מצויר מעליה. שחזור הטופס
  בקוד היה מייצר מסמך שהבנק עלול לפסול.
- **מיקומים בטופס הם `left` פיזי ולא `inset-inline`.** הדף `dir="rtl"`, ושם
  `inline-end` הוא השמאל — זה הפך את כל השדות בסבב הראשון.
- **אין הבטחת זיכוי מס באתר.** בדיקת `GetMosad` מחזירה `As46: 0` — למוסד אין
  אישור סעיף 46.

## מה עוד חסר (`todo` בקבצי ה-JSON)

- זמני מנחה, ערבית ותפילות שבת — לא ידועים, `config.json` מכיל רק שחרית ודף יומי.
- טלפוני גבאים — `config.contacts` ריק, ולכן אין איך ליצור קשר מהאתר.
- מרווח הדלקת נרות 40 דק׳ — ברירת מחדל לפי אזור ירושלים. **טעון אישור.**
- מחירי המקומות לימים הנוראים — ברירת מחדל שקבעתי. `draft: true` עד לאישור.
